# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Anthropic API key for the LLM (parsing + persona only; never the math path).
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides (defaults live in src/howmid/config.py):
# HOWMID_DB=data/howmid.duckdb
# HOWMID_MODEL=claude-haiku-4-5
# HOWMID_MAX_TOKENS=1024
